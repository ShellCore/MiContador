<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_accounts"
            android:icon="@drawable/ic_accounts"
            android:title="@string/accounts" />

    </group>

    <item android:title="@string/menu_categories">
        <menu>
            <item
                android:id="@+id/nav_income"
                android:icon="@drawable/ic_menu_categories"
                android:title="@string/income" />
            <item
                android:id="@+id/nav_expense"
                android:icon="@drawable/ic_menu_categories"
                android:title="@string/expense" />
        </menu>
    </item>
</menu>